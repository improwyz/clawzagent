# Deprecated — use `web/` instead

The static HTML shell in this directory is **deprecated** as of 2026-05-30.

**Desktop and mobile** Tauri apps load the React dashboard from [`web/`](../../../web/) via `frontendDist` in `tauri.conf.json`.

Branding assets: copy from [`web/public/branding/`](../../../web/public/branding/) when updating icons. Do not add new features here.
